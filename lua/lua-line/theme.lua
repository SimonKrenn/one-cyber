local colors = require('lush-theme.colors');

return {
	normal = {
		a = { fg = colors.fg, bg = colors.green },
		b = { fg = colors.fg, bg = colors.light_blue },
		c = { fg = colors.fg, bg = colors.blue },
		x = { fg = colors.fg, bg = colors.blue },
		y = { fg = colors.fg, bg = colors.light_blue },
		z = { fg = colors.fg, bg = colors.light_blue },
	},
}
